{
  "type": "string",
  "enum": ["default enum value"],
  "uri": "/{path}?value={queryValue}",
  "meta":
  {
    "fieldId": "meta.key",
    "fieldValue": "data",
    "placeholders":
    {
      "path": "blah",
      "queryValue": "foo"
    }
  }
}
